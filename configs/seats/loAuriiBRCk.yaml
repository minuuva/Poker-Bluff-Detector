# Seat map for session loAuriiBRCk (Feb 2026, "Alex Foxen, Suited Superman
# & Nik Airball").
#
# v2 schema: per player, a generous search region covering every zoom level
# of the shots that frame them, plus face-chip reference timestamps with the
# approximate face center x (full-frame px) to disambiguate multi-face
# frames.
#
# SUITEDSUPERMAN is the man in glasses and a gray CoinPoker t-shirt:
# his panel flips to ALL IN $10,175 at t~5697 exactly as he reaches into
# his chips, and after losing that flip he is standing behind the table
# by t=5913. The old v1 map also credited him with the shot at t=5910,
# but the HUD there shows a hand he is not in (Foxen/Francisco/Airball)
# and the face at that spot is a different player in a blue Puma hoodie;
# that reference is dropped.
#
# More SUPERMAN refs plus AIRBALL refs get added after this session's
# hands assemble (commit-moment identification pass); Airball's Feb look
# may differ from the Jul session.

seats:
  SUITEDSUPERMAN:
    search: {x: 850, y: 0, w: 1070, h: 900}
    face_refs:
      - {t: 5692.0, cx: 1400}
      - {t: 5701.0, cx: 1336}
